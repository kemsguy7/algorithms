

/* 
Write a function called samw, which accepts two arrays. The function should return true if every value in the array has it's corresponsing value squared in the second array. 
The frequency of values must be the same


E.g same([1,2,3], [4,1,9]) //true
same([1,2,3], [1,9]) //false
same([1,2,1], [4,4,1]) // false (must be same frequency)
*/

